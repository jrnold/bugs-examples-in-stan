model{
  for(i in 1:n){   ## regression model for y
    mu[i] <- beta[1] + beta[2]*x[i]
    y[i] ~ dnorm(mu[i],tau)
  }
  ## priors
  beta ~ dmnorm(b0,B0)
  sigma ~ dunif(0,100)
  tau <- pow(sigma,-2)
}
